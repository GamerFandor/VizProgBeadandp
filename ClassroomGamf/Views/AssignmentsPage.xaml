<Page x:Class="ClassroomGamf.Views.AssignmentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClassroomGamf.Views"
      mc:Ignorable="d" 
      Title="AssignmentsPage" Background="White" MinHeight="60">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel VerticalAlignment="Center" Background="#2C3E50">
            <!-- Vissza nyíl -->
            <Button Content="←"
                Width="30" Height="30"
                Margin="10,0"
                Click="BackButton_Click"
                DockPanel.Dock="Left" />

            <!-- Csoportnév -->
            <TextBlock x:Name="GroupNameText"
                   Text="Group Name"
                   Foreground="White"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="10,0"
                   VerticalAlignment="Center"
                   DockPanel.Dock="Left" />

            <!-- Navigációs gombok -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                <Button Click="FeedButton_Click" Content="Feed" Margin="5,0"/>
                <Button Content="Assignments" Margin="5,0"/>
                <Button Click="MessageButton_Click" Content="Message Teachers" Margin="5,0"/>
            </StackPanel>

            <!-- Jobb oldali profil gomb -->
            <Button Content="👤"
                Width="30" Height="30"
                HorizontalAlignment="Right"
                Margin="0,0,10,0"
                DockPanel.Dock="Right"
                ToolTip="Felhasználói fiók" 
                Click="UserButton_Click"/>
        </DockPanel>


        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="20">

                <!-- Dummy Post -->
                <Border Background="#EFEFEF"
                Padding="15"
                CornerRadius="10">
                    <StackPanel>
                        <TextBlock Text="Feladat 1"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,5"/>

                        <TextBlock Text="Teszt Felhasználó • 2025.05.02"
                           FontStyle="Italic"
                           Foreground="Gray"
                           Margin="0,0,0,10"/>

                        <TextBlock Text="Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"/>

                        <!-- Komment -->
                        <Button Content="Feladat feltöltése"></Button>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
